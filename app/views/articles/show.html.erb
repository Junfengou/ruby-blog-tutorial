<h1><%= @article.title %></h1>
<p><%= @article.body  %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy this post", article_path(@article), method: :delete, data: {confirm: "Are you sure?"}%></li>
</ul>

<h2>Comments</h2>
<!--partial-->
<!--Still don't understand how Rails know exactly which partial I'm refering to-->
<%= render @article.comments %>

<h2>Add a comment:</h2>
<!--partial-->
<%= render 'comments/form' %>
